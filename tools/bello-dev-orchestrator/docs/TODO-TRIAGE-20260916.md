# TODO整理（2026-09-16）

通常画面は「今すぐ対応が必要」だけを初期表示する。後で確認、AI対応待ち、アーカイブは別タブ。選択タブはブラウザに保存する。5件で切り捨てる制限は設けない。

元の323件: 今すぐ0、後で77、AI対応待ち105、アーカイブ141。アーカイブの内訳は過去タスク138、重複3、その他失効0。既に完了・取消だった7件も履歴タブへ含めるため画面上のアーカイブは148件。

今すぐとは、実行中/キュー中または最後に実行した対象タスクを止める本人操作・重要承認。導入時点では進行中タスクがなく、過去のawaiting_userタスクを一律に現在の承認とは扱わない。過去の本番デプロイ承認等は後で確認へ保持し、対象タスクを再開すると再分類する。

元のTODO・タスク状態・関連履歴を一括変更せず、todo_triageとtodo_ai_queueへ分類・担当・理由・統合先を追加保存。アーカイブは表示上のアーカイブで元の依頼を削除しない。同一対象・具体的な件名の重複は詳細条件を統合先へ保持する。短い汎用件名は操作・完了条件の一致も必要。

AI対応待ちの依頼は、対象タスクが実行されるとAgentへの指示に組み込む。通常作業だけを理由とするrequest_user_actionは上限付きの自動再試行へ進む。独立検証を含む完了ゲート成功後に実際のAI処理完了を記録。過去タスクの一斉再開や105件の実行完了を意味しない。未実行を「AI処理中」や「解決済み」と表示しない。

不明な仕様・明示的な手動審査・対象環境の不明な操作は自動承認しない。本人認証、本番反映、本番データ変更、破壊的migration、重大権限変更、実出品、課金を実行する権限は本変更に含まれない。

検証: 164回帰テスト成功後、追加ケースを含む関連28テスト成功。DB再オープンで分類・AI待ち状態の保持、再分類の冪等性、タスク状態不変、重複統合、過去タスクの再表示、重要承認の保持、通常作業の再試行を確認。
