# 実Codex → 独立テスト → Amplify 実通し検証（2026-09-16）

## 結果
タスク task_d2fe05c1bcab989591 は実Codexがindex.htmlを編集し、Orchestrator自身の独立テスト、自動コミット、専用Amplifyへのアップロード、HTTP内容照合を経て completed になった。

- AWSアカウント: 203918843421 / us-west-2
- 新規stack: bello-orchestrator-smoke-20260916
- 新規app: d22lq9g4o2zu1o / bello-orchestrator-smoke
- branch: preview-orchestrator
- job: 1 / SUCCEED
- commit: e46699d759679c1cccd70760193e124bf9d518cf
- HTML SHA256: aad1b9ec013031fedbacddbd349315ecef2695c1ec0bb119bb13452588de0fae
- 公開URL: https://preview-orchestrator.d22lq9g4o2zu1o.amplifyapp.com/
- HTTP 200と成果物の完全一致をstatic_http checkpointに保存。ブラウザ表示でもBELLO E2E VERIFIED 20260916を確認。

## 分離
静的WEBホスティングのみ。Cognito、DynamoDB、業務S3、backend、IAMロールは新設せず、既存のものも参照しない。Amplify管理のアップロード先に165バイトのHTMLを送った。Git接続・環境変数・実行ロールなし、自動ビルド無効。ZAICO・LINE・Mercariなどは一切呼び出さない。

AWS操作は既存Belloプロファイルを使用した。このプロファイル自体のAdministratorAccess権限を縮小したわけではない。今回IAM追加・変更はゼロ。アダプターが使うAPIは対象アプリへの読み取り・create-deployment・start-deploymentのみ。

## 実装
- staging.mode=static-smokeで専用アダプターを選ぶ。通常Git RELEASE処理は維持。
- 対象名、アカウント、branch、WEB、環境変数なし、backendなし等を反映前に照合。
- worktree、独立検証結果、clean commit、単一index.html、文字主体の厳密なHTML形式を要求。
- ZIPの署名付きURLはログに保存しない。job IDはアップロード前に保存。起動応答不明時は再送しない。
- 公開HTMLのSHA256が一致するまでcompletedにしない。
- CodexのWindows sandbox=elevatedを明示。最初の実行は設定不足で編集不能となり、独立テストが失敗を検出。権限境界を維持した設定修正後、同一タスクの2回目で成功した。
- 反映完了時に古い待機理由・再試行時刻を消す。

## 検証範囲と制限
158回帰テスト成功。その後の完了理由クリア修正は関連23テスト成功。PowerShellは変更なし、直前の構文検査成功を再利用。

実通し試験は通常Orchestratorクラス、実CodexRunner、実IndependentVerifier、実AmplifyStaticDelivery、実SQLiteを使用。業務キューとは別のdataRoot・単一HTMLの専用Gitリポジトリで実施。審査はこの文字変更だけの決定的な機械判定であり、Claude/OpenAIのAI審査を実接続した試験ではない。受付はRepo.createTask経由で、ダッシュボード入力やWord取り込みの実試験ではない。

最初のWindows設定修正と再キュー投入は本調査で実施した。Orchestratorが自分の起動コードを自動修復したという意味ではない。実業務アプリやバックエンドを含む全自動運用の安全性を保証する試験でもない。

## 再開用
実行記録: C:/Users/win/Documents/Codex/bello-live-smoke/result.json
専用DB・worktree: C:/Users/win/Documents/Codex/bello-live-smoke/data
試験ドライバー: outputs/BELLO-live-smoke-driver.mjs（実施時点の監査用コピー、相対importは元のtoolディレクトリ基準）
既存の主サービスと業務キューは一時停止を維持。主設定のstaging有効化はしていない。専用構成だけで完了を確認した。
